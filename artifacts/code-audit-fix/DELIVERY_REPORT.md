# Uquant 代码审计修复：候选分支交付

仓库：`ychenracing/uquant`。候选分支：`codex/code-audit-fixes-20260922`。
实施基线：`7bc5cd5e20038c94ab5cf556ce107104692236ac`；原审计：`58d4b9cf7c91b04bf98e970a2ce546f66ee5112d`。
授权：修复原审计 F01–F14，保存在候选分支，不合并 main。没有进行实盘交易、策略调参或收益优化。

## 修复对应表

| 编号 | 已实施处理 | 关键边界 |
|---|---|---|
| F01 | 券商快照必须明确提供现金 | 合法零现金仍接受；缺失、非有限数值拒绝且账户不变 |
| F02 | 快照、成交及可卖日期按解析后的日期比较；新增日期规范化 | 接受旧合法 ISO 形式，不回写已有历史记录 |
| F03 | 日常与独立同步入口复用现有严格 JSON 解码器 | 拒绝重复键、非标准常量及非对象顶层 |
| F04 | 对账和决策使用同一绑定配置；account-sync 支持 --config | 不自动迁移不一致配置，不退回默认配置放宽限制 |
| F05 | 更换行情权威时失效全部相关引擎派生缓存 | 同一对象不重复清缓存；市场字典引用保持有效 |
| F06 | 评分 profile 标签采用实际评分时读取的机会状态 | 不改变现有评分/机会分类时序，不重算另一种策略 |
| F07 | 风险清理改读结构化原因；旧事件保留读取兼容 | 原因优先级及触发条件保持；展示文案不再控制当前状态 |
| F08 | 普通转仓及修复来源使用类型化账户视图，冻结观察统一提交 | 不新增第二账户，不改变持久化键，不复制假设资金或授冠权限 |
| F09 | 组合、领涨和战略类改为显式方法声明 | 保留现有实际策略实现、旧类路径、MRO 和必要 pickle 兼容 |
| F10 | 引擎改用普通方法委托，应用包只做显式导出 | 保留有效替换入口，不再运行时重写函数注解/docstring |
| F11 | 机会分类使用具名结果与具体类型 | 保持原分类条件、顺序及账户状态变更 |
| F12 | 合并两条实质相同的治理测试入口 | 保留同一实质验证和原有负向用例，不重写冻结证据 |
| F13 | 生产战术探针移除误导性的 research 命名 | 仅改当前函数名及观察标签，历史原件不修改 |
| F14 | 清除冗余字符串身份比较及生产内无意义的配置选择跳转 | 仍有兼容价值的旧签名保留，池大小不选择策略 |

八个固定 policy 的经济规则未借本次修复删除或调参。“13 个公开设置”不等于仅有 13 条生效策略规则。必要账户、安全、费用、T+1、身份及事务发布校验保留。

## 验证与证据

代码与验收保全提交：`52e93b69f14bd6be41b9047cebf3183df6f903c6`；远端回读回执提交：`e3280352fd15ca42778b7edb69167618dc745424`。
最终生产代码指纹：`ebae4b4d1cf816d953c7d10a066a0d91db584f126039c070f4a41f9b82c97674`。

- GitHub Python 3.12.13 锁定环境：198 个定向用例通过。197 个来自 run `35676785570`，最终 run `35677354320` 新运行完整反射/MRO/pickle/导入模式检查，并验证前述证据的适用性。
- 22 个受影响生产文件的 strict mypy 通过；最终 Ruff 与差异空白检查通过。
- 最终仅调整 `application.__all__` 的声明排序；导出成员、函数体与其他受测文件不变，已通过 AST 及逐文件哈希核验复用证据，没有重新跑未受影响的 197 个用例或完整配对。
- 配对回放 run `35676785570`：2023-01-03 至 2023-03-31 三标的窗口 59 个交易日、4 笔成交；2026-06-01 至 2026-08-05 七标的窗口 47 个交易日、6 笔成交。两个窗口逐日现金、持仓、目标、订单、成交、费用及比较范围内风险控制字段完全一致。
- 配对的最终权益分别为 4,269,102.12359264 和 2,111,421.3070430905，基线与候选相同。这里的数值仅用于经济行为回归，不表示完整窗口收益能力或未来收益。
- 配对原始候选代码指纹为 `63117a6dfb67e6fb04f00ddf4144d22ad139bb2d9ae47877cc5c1636711c226b`。后续导出排序的行为中性证明保存在 `FINAL_VERIFICATION.json`；没有把这份原回放改贴最终指纹冒充新运行。

最终结论入口：`FINAL_VERIFICATION.json` 和 `VERIFICATION_SUMMARY.json`。早期失败与原运行结果保留，不把失败记录改成通过。

完整原始日志按运行 ID 保留在 `logs/`，并含长度与 SHA-256。`VERIFICATION.json` 记录实际测试文件身份；触发提交之后由 runner 施加修改，不能仅以触发 SHA 冒充测试源码。`REMOTE_READBACK.json` 记录提交后的远端逐字节、SHA-256、Git blob 回读。

配对回放的基线和候选使用同一锁定环境、冻结行情与配置，通过真实 ProductionEngine/AccountState/ExecutionPlanner 链路执行。原始决策和账户逐日保存，经济比较仅剔除本就随源码变化的身份字段及本次明确修复的观察标签；不改变交易、成本或风险指标。它是有边界的回归证据，不是完整 Absolute/34-LOO 或所有历史场景验收。

## 保留与限制

本次交付只覆盖原审计列明的 14 组问题，不是完成了此前尚未完成的全仓逐文件审计。没有宣称恢复收益、提高收益或通过完整泛化验收。完整仓库 CI、分支覆盖率、其余历史 AST 形状治理检查及完整 Absolute/34-LOO 经济矩阵本轮未运行；候选保存不等于 main 发布验收通过。

账户 schema 未整体重写；类型化视图覆盖这次确认的普通转仓、修复来源与冻结观察接口，并不声称所有历史状态字典都已消除。必要的旧导入/序列化兼容保留；未恢复运行时文档空格或函数注解伪装。

没有合并 main，也没有授权自动合并。候选源码、修复脚本、测试命令原始输出与配对逐日原件保存在 GitHub；临时 runner 和本地容器不是唯一恢复来源。本次临时执行/传输工作流在交付后移除，原脚本和失败证据仍可从分支历史追溯。
