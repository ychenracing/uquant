# PR83：完整检查、集成修复与证据

## 范围

承接代码审计 F01–F14；用户授权运行完整 Engineering、覆盖率、历史 AST 治理与 Absolute/34-LOO，修复已暴露问题后 squash merge。超过十分钟的运行不作为等待项，未完成结果不标记通过。实际合并状态和 main SHA 以 PR #83 的 GitHub 记录为准。

## 已修复

- 更新 Absolute 加载器的源码清单锚，使其与实际生产成员对应；配置、冻结行情、证券池、seed 和经济阈值未修改。
- 为普通状态视图登记真实模块所有者；消除遗漏登记引发的治理检查连锁失败。
- 历史搬迁检查识别显式方法委托，并严格核对签名、默认值、实际目标和逐参数转发；保留错误 owner、交换/遗漏参数、额外状态写入等负向断言。
- 源码变异按 token 定位，避免格式化导致变异没有触达生产语句。保留原经济与权限断言，不把空格或注释外观当作经济行为。
- 将内部结果类型正式命名并显式导出，修复私有跨模块依赖；历史公共 API 工件未覆写，授权演进在独立 delta 中验证。
- 旧恢复失败夹具补齐生产实际提供的结构化风险原因。没有恢复用英文文案控制账户清理的旧实现。
- 导入环检查区分立即执行、延迟调用与 TYPE_CHECKING，保留真实启动环的负向用例；重复 helper 分析先证明具体委托完全等价，不忽略未知重复实现。
- 覆盖率在测试分片失败后独立汇总，但仍要求两份真实原件及 85% 门槛。
- 日志收集改为二进制流式下载并验证，修复 gh 不支持参数导致的收集器失败；签名下载不转发 GitHub Authorization。

## 实际验证

完整 Engineering 运行 `35679019524` 已执行质量、严格类型、构建、安全、Windows、全部五个测试分片。旧失败记录保留，不改写成绿色。

应用左半分片结果为 2735 通过、2 失败；两个失败均为上述旧风险夹具。它们在 `35683876795` 已通过，又在当前源码 `2cfe78cc3a8ec1e9fb07068cb3822d17cf592782` 的运行 `35687493317` 中 2/2 通过（2.79 秒）。应用右半分片通过。完整应用分片覆盖率原件由 `35685639978` 聚合：**87.4250299880048%，门槛 85%，通过**。

历史 AST/API/所有权问题的分批重验：`35683876795` 两组分别 33、38 通过；`35684675683` 有 55 通过并保留最后两个失败；`35685639978` 最后两个失败及八个新增负向/健全性用例全部通过。部分用例被定向重复执行，这些数字不是不重复测试总数。初始因模块登记缺失而提前失败的数百个变异用例，已修复共同根因；不能因此宣称它们在当前 HEAD 下逐项完成新的全量重验。

新一轮完整 Engineering `35687496428` 已提交，Windows 通过，其余最新查询仍排队；不等待，也不标为通过。新的 Grant/Ownership/Absolute 工作流同样已提交。这里明确区分完整检查已运行、已知失败定向修复和最新 HEAD 整体绿灯，三者不是同一声明。

## 完整 Absolute/34-LOO

运行 **`35679019431`** 已完成，结论 success。最终原件：

`35687493317/ABSOLUTE_FINAL_ORIGINAL.json`

报告封印 `a23524983483bb8a93adb4e6839ec725e49daee06a195d89c00da712ceb6a2b8` 已按 canonical JSON 重新核验：34/34 有效，缺失 0、重复 0、回放错误 0，七个组件全部通过；runner_success、capability_pass、passed 均为 true。独立 Strategic Grant `35678573838` 和完整 Strategic Ownership `35678573831` 也已通过。

报告实际生产者是 PR 合成提交 `9632d4cb770502c33934f5bdc5df556b69921936`，分支输入 `6d4f8ab1cd72b5a7471bd2e3b92b0e035f278286`，生产指纹 `ebae4b4d1cf816d953c7d10a066a0d91db584f126039c070f4a41f9b82c97674`。配置 `4d9c3495556d78e24f629355bb2bedbd4f3e823a53cf30499c9f18e04d53fda6`。不将原结果改贴最终合并 SHA 冒充新运行。

原生产者之后，经济源码成员仅有内部结果类 `_DecisionResult` 改名为 `ObservedDecisionResult` 及同一类的显式导出。对实际源码逐文件比较及精确改名后的 AST 对照均一致，未归一化交易条件、资金、风险、订单、成本或数据。当前生产指纹为 `e84bd39fae1a38a61f6a331b6900b96dc5200395587725ac6d61af56929c311a`；复用依据见 `REUSE_AND_READBACK.json`。这支持复用已验证经济路径，不表示源码 SHA 相同。

## 原件保全与边界

修复、原失败日志、定向复验和覆盖率原件均在本目录分运行保存。最新收集器 `35687493317` 成功，四个旧失败 job 的完整原始日志已按字节保留；846 个源码文件的下载长度、SHA-256 和 Git blob 已核验。最终报告原件与其原始 ZIP 已提交候选分支。

八个完整 Absolute 分片原件仍位于该运行的 GitHub Actions artifacts，登记的到期日为 **2026-12-21**；这不是永久 Git 备份。没有用最终摘要替代分片原件，也未声称本轮重新下载并独立重算全部分片账户。临时维护工作流在交付提交移除，脚本与历史运行仍可追溯。

当前没有新增策略、调参、冻结标准修改或实盘操作。已知失败有根因修复和受影响路径证据；最新完整 CI 排队结果及合并后触发的 CI 仍须如实保留未完成状态。本记录不宣称未来收益保证，也不宣称全仓不存在其他尚未发现的问题。
