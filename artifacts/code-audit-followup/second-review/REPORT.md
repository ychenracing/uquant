# 第二轮整体检查

检查基线：`f5758619c79a5445c51986b0ff9f15aa72851752`。本地验证代码提交：
`2ea1189`。发布时使用同一文件树；GitHub 连接器可能生成不同提交身份。

本轮重新扫描全部 342 个当前实现文件（生产 249、研究 63、脚本 19、工具 11），
结合已有 710 个 Python 文件清单、全仓静态检查、调用搜索和最新 Actions 日志审查。
人工深入检查的是异常命中、兼容入口及改动路径，不能把静态扫描说成逐行人工审查。
“不像人类写的”不能客观证明代码来源；本轮以真实维护负担、错误和冗余作为修复依据。

## 修复

1. 删除 `holdout` 模块伪造 `__file__` 的兼容代码。全仓搜索只找到两个测试依赖该
   假路径，现在测试直接从自身位置定位仓库。实际模块路径恢复真实值。
2. 删除报告函数的 18 个 `Any` 参数传递。按贡献和运行成本分成两段，直接读取
   同一份已验证的归因数据；五种桶格式化复用一个小函数。输出与基线逐字节相同。
3. 合并两份完全相同的 Markdown 转义函数；两个日志格式继续保留各自字段和表头。
4. 将两份研究工具复制的数值校验、A–E 池读取和 Python 源码哈希收拢到已有的
   `research.window_matrix`。保留原来的错误、池顺序、哈希字节和内部导入别名。
5. 给九个提取出的辅助函数补全实际参数和返回类型，覆盖回测结果、账户归因、
   参考广度、Sentinel 证据、校准和只读 CLI。七个生产文件去除类型声明及导入后，
   可执行 AST 与基线完全一致；mypy 331 文件通过。

## Actions 根因与修复

最新有结论的同源码运行 `35695668278` 中，portfolio、应用右分片、质量、Windows、
安全已经通过，但以下两个检查仍失败，原始日志完整保留在本目录：

- `106641875572`：CLI 语义检查没有展开新提取的日常执行函数。现在先验证签名、
  调用参数、唯一调用、无装饰器和文档位置，再将函数体展开，与冻结原实现比较。
  新增三种反向用例：反转状态检查、交换账户/券商路径、将文档替换成可执行副作用。
  三种修改都被拒绝，没有跳过原来的语义证明。
- `106641875534`：风险历史所有权清单漏记已有的 `uquant.models.ordinary_state`。
  从冻结树路径与当前所有权的差集确认只缺这一项，补全精确清单。历史源码读取在
  此测试中先筛选 `uquant/`，不再先读取全部历史研究和测试代码后丢弃；检查范围不变。

原风险测试通过（513 秒），缩小无关读取后的完整同一测试通过（151 秒）。上一轮
“超时未验证”的项目本轮已得到通过结论。没有修改冻结阈值、预算或经济验收条件。
历史失败运行不会被删除，也不会因后续修复自动变绿。新提交仍需由新 Actions 验证。

## 验证

| 检查 | 结果 |
| --- | --- |
| 全仓 Ruff / 差异空白检查 | 通过 |
| mypy | 331 个文件通过 |
| CLI 治理、holdout、两种日志、两份窗口研究工具 | 90 项通过 |
| 报告、校准、公开 API | 28 项通过 |
| CLI 语义证明与三种反向用例 | 4 项通过 |
| 风险历史完整用例，限定读取范围后 | 1 项通过 |
| 复杂度预算 | 5 项通过 |
| 经济归因报告改前/改后输出 | 逐字节相同；损坏证据仍拒绝 |
| 七个仅类型改动文件 | 可执行 AST 相同 |

用例数存在重叠，不能相加当作独立覆盖数。本轮没有重复完整经济矩阵；没有策略行为
修改，复用上一轮的经济回放证据，报告/研究输出和实际变动路径另行验证。
没有等待超过十分钟的 Actions，也不把排队或运行中的检查记为通过。

## 有意保留的兼容与局限

- 旧账户迁移、旧日志解码和冻结证据投影有实际持久化输入，删除会破坏读取或归因。
- 公开导入入口和类的历史模块身份服务反射/pickle；保留薄转发，避免复制策略实现。
- holdout/观测 CLI 的有限依赖注入继续用于事务失败、锁和回滚验证，未重新引入全局
  模块篡改或任意属性转发。
- 两处空异常处理分别是失败回滚时的次级读取、云记录器可选 Git 诊断，保留原始异常
  或允许诊断缺失；并非忽略交易或发布失败。

这些兼容功能不等于已经无用的残留。没有依据声称所有兼容代码都可删除，也没有
声称任何未来审查都不会再发现问题。本轮确认可安全修复的问题已处理。

`validation.json` 保存各次命令结果；完整原始日志、事件和检查点打包在两个二进制
分片中。按清单顺序拼接 `validation-logs.tar.gz.part*`，核对总长度和 SHA-256 后
即可恢复原件。上传回读另核验各文件原字节和 Git blob 对象身份。
