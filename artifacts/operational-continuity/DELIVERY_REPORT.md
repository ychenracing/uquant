# 工程收敛交付

## 范围与结论

从 main `58d4b9cf7c91b04bf98e970a2ce546f66ee5112d` 恢复，遵守 AGENTS.md，采用 PonyTail。
本轮停止冻结样本收益搜索；没有新策略、参数调整、验收放宽或 PR80/81 失败实现复活。
工程改善不代表收益改善。

## 已复现并修复

| 缺陷 | 真实调用与后果 | 最小修复 |
|---|---|---|
| 日期在追加之后检查 | production_observation → holdout_runtime → service → snapshots；错误 daily 日期仍留下不可变 session | expected_session 传到原追加函数，在安装和幂等返回前检查 |
| 可写账户与输入载体未完整隔离 | 生产账户可与回放期初账户、券商载体或备份重叠；报告可指向冻结行情、Holdout 数据或 replay checkpoint | 复用已有路径与硬链接比较，对这些载体在任何追加前拒绝 |
| 共享观察证据没有跨账户互斥 | 两个进程用同仓库不同账户，可同时进入写入共享 Holdout/报告的事务 | 原有文件锁改以仓库为身份，保持释放与异常语义 |
| Absolute 配置身份标注陈旧 | 合同加载器复制旧政策配置摘要，单元、缓存、分片和 final 都继承错误标注 | 唯一出口从实际 DEFAULT_CONFIG 计算；原独立配置预检和冻结政策保持不变 |

日期与路径回归在修复前 7 失败/1 通过；跨账户进程锁回归在修复前失败。
配置身份回归在修复前证明原标注与实际配置不同。原失败日志保留。

复查的日常路径为 CLI → 券商对账 → 唯一决策 → 账户保存 → 双格式报告发布。
已有报告发布失败恢复、真实订单身份、部分成交、取消待确认和 Journal checkpoint 机制保留；
没有为复杂度删除战略所有权、恢复权或风险职责。审查范围是这些真实入口及共享依赖。

## 经济证据与验收

经济源码 `economic_decision_v1` 前后均为
`27131f3e591fbe1ace0ea2f1abf02bbc98b5242f1b60589f474ac189c5971e40`。
配置、锁文件、冻结数据、证券池与政策没有改动。见 [复用身份](ECONOMIC_REUSE.json)。
本轮不运行收益比较矩阵；新验证针对观察事务和证据身份。

[main 完整 Absolute 运行](https://github.com/ychenracing/uquant/actions/runs/35628142765) 的
[原始报告](ABSOLUTE_MAIN_REPORT.json)已取得并核验 canonical seal
`1f2af7079ab4f0b5554a8e85e207f54936a5ed8a03b924ca786417bfcbf17873`。
报告绑定 main、attempt 1 和八个分片摘要，34/34 单元有效，无缺失/重复/回放错误。
其原始 runner_success/capability_pass/passed 均为 true：正收益比例 1，p10 终值 26.0244，
p90 回撤 0.2978274，恢复健康观察 20/40/60/60。此前 PR80 交付时未完成的工作流现已完成，
历史文件不回写。八个大分片原件仍在该运行的 Actions artifacts，未声称本轮下载并重新聚合全部原始账户。

但原报告记录的配置 `adf8c123…` 是冻结政策旧标签；其生产者源码独立要求的实际配置为
`4d9c3495…`。不能忽略该身份缺陷，也不能把它叫作回测失败或经济不达标。
保留数值与原通过声明，原件不改标签/不重封；修复后的严格配置身份不接受这份旧标签作为新运行。
新的完整严格身份矩阵若未完成，必须标记未完成，而非复用为新 HEAD 通过。

已知经济局限包括起始日与主导证券敏感性、前复权与真实公司行动的差距，以及实际排队、
停牌和冲击成本。PR80/81 的候选 NO_GO 仍然成立，不因本轮工程修复改变。

## 日常操作与未来观察

[运行手册](../../docs/OPERATIONS.md)与[Holdout 手册](../../docs/HOLDOUT.md)补齐了
系统 order_id → 稳定 plan-id → 真实部分成交/跳过 → 后续对账的对应方式。
无订单日保留原日报；人工 skipped 不冒充券商取消；回放决策不冒充真实账户建议。
不新增自动交易、调度或第二套平台。

静态与本地 Lane 报告均为零观察、正式分数 null。三个已登记 Lane 保留旧源码身份，
不自动绑定当前 main。本轮没有真实券商快照、连续账户与新 session，未执行实盘 daily，
未追加真实 Holdout、未新建策略 Lane，也未补造历史建议或成交。
当前版本正式前瞻登记、连续账户匹配及真实 20/40/60 session 验收仍未完成。

事务锁只覆盖同仓库的 production_observation；直接 CLI、外部编辑或跨 checkout 仍需单一
写入者。失败恢复沿现有备份、receipt 和 ready 报告核对，不删除已追加 session，不重跑补报告。

## 验证与发布

最终检查与远端身份在 [验证记录](VERIFICATION.json) 和本次 PR 中记录。
仓库保护与实际必需检查按远端状态执行，不强制合并、不修改规则；自动 CI 的未完成状态
不计为通过。工程结果不作收益改善声明。
