# Recovery implementation convergence — preregistration

Authority: current user implementation mandate. Base main: dd5ef43931c3e316f3808d4ef04f34e3953be194.

A only: retire the unreachable recovery-anchor substitution implementation and its dynamic binding/target builders. Preserve the public policy class, MRO, pickle identity, historical attribution and account schema. Preserve every currently reachable recovery, ordinary, allocation and risk policy. No config or economic threshold changes.

Evidence: production references to `_recovery_anchor_substitution` are declaration/binding only; current pipeline uses `allocate_confirmed_recovery`. Existing topology test requires zero calls. Verify dynamic references before removal and forbid reintroduction. Expected reduction: one obsolete policy implementation (~385 lines), two target builders, two carrier types and the binding layer; not a claim of simplifying all active strategy rules.

Acceptance, registered before candidate execution:
- Remove the obsolete implementation, no parallel replacement, config or persistent state.
- Zero changes to remaining production function bodies, except removal of the obsolete TYPE_CHECKING signature and binder.
- Exact paired comparison of intermediate allocation evidence, ordered targets, reasons, account state, orders/fills and final economics. Allow only explicit source-identity metadata normalization; no numeric tolerance for economic differences.
- Use the existing architecture daily trace runner on its three real-data paths (early entry, late-2024 rotation, recent shock), plus current recovery/continuation, funds, attribution and serialization boundary tests. Expand only for a discovered gap. Previously used historical data is research, not holdout.
- Wealth, drawdown, turnover/cost and operation count deltas must be zero on compared paths. Any economic difference blocks merge.
- Preserve historical original artifacts unchanged; adapt only current tests tied to the removed private implementation. Keep actual order identity/attribution tests.

B: no evidence-supported economic candidate identified in PR76/77. Do not repeat failed strategies or claim A achieves B. No new return/risk trade-off or relaxation of old contracts.
