# 恢复实现收敛交付

A（行为保持的工程简化）已通过本轮验收；B（经济改进）没有充分依据实施。两者分别评价。本轮只实施 A；PR76/77 的失败证据不支持新经济候选，未重复试验、调整门槛或把工程清理解释为收益提升。

## 范围与实质变化

基线 main：dd5ef43931c3e316f3808d4ef04f34e3953be194。事前合同见 ACCEPTANCE.md；远端初始登记提交 357c45f917305a812ec1abe9115939c0a899543e。

当前生产入口 `PortfolioAllocator.allocate → _allocate_strategy → allocate_confirmed_recovery` 已不调用旧 `_recovery_anchor_substitution`。删除其完整生成器、两个上下文/候选类型、两个专用目标构造器、动态方法装配及类型声明；生产四个文件共净减少 **481 行**。剩余生产函数体与基线 AST 完全一致，配置、源码登记清单、账户字段及序列化格式未改变。详见 COMPLEXITY.json。

这是一个旧实现家族的实质退役，减少误接入旧策略与维护两套恢复实现的负担；不是全系统架构改造。活跃经济规则、持久状态、配置数量及生产调用层次没有减少。公共 RecoveryPortfolioPolicy 类/MRO/pickle 身份保留，旧 substitution 模块仅保留一行命名空间说明，三个旧常量为配置身份兼容保留，文档明确它们不驱动当前替换准入。

保留现行恢复组准入、锁定、待成交延续、持仓恢复、普通买入互斥、退出和风险政策。历史恢复替换订单的身份、关联、序列化与执行保障继续保留；只删除三个直接调用退役私有策略的测试和对应 AST 原样搬迁测试。历史 artifacts 未改写。

## 行为证据与身份

1. 基线原生引擎四条真实数据路径共 159 个交易日，记录每次分配器完整输入，再由候选处理同一输入。目标顺序、原因、完整账户、风险证据（包括资金预算、承诺、提议、持仓及待成交状态）逐字节一致。未删字段、未用资金容差，也没有把两个源码标成同一身份。原输入中的 source-linked 身份保持为基线测试夹具；候选源码另记。见 BEHAVIOR_COMPARISON.json。
2. 既有现行引擎三条短路径共 60 日的完整架构追踪通过；公共类/MRO/pickle、导入模式、历史原件保持检查通过。
3. 对受影响的 2024 恢复路径另跑独立、绑定候选源码的原生回测，逐项比较净值曲线、收益、回撤、成本、换手、订单数及操作日。最终结果以 NATIVE_COMPARISON.json 为准，未把旧结果重贴当前 HEAD。

原生恢复期实测（2024-01-02—2024-06-03，34 标的，99 日）：

| 指标 | 基线 | 候选 | 变化 |
|---|---:|---:|---:|
| 成本后财富倍数 | 0.95292965× | 0.95292965× | 0 |
| 最大回撤 | 10.140134% | 10.140134% | 0 个百分点 |
| 订单／操作日 | 20／13 | 20／13 | 0／0 |
| 换手（成交额／当日权益求和） | 3.69276540 | 3.69276540 | 0 |
| 费用及滑点／当日权益求和 | 0.540300% | 0.540300% | 0 |

已测路径内最差退化为零；未运行的其他窗口不新增测量结论。

全部窗口均为已使用研究数据，不是未见样本，不证明未来产业周期泛化。BASELINE_METRICS.json 的其余三条历史路径指标只用于说明测试路径，不作为新经济改进结果。

## 验证及失败保留

初次定向检查 243 项通过、9 项失败：退役文件/源码清单适配问题及测试夹具漏填 origin_lifecycle。已修正；复验中发现误改历史迁移清单，恢复原清单。多断言复验分别因重复架构快照和导入扫描超过 360／180 秒预算，记录为超时而非通过；最终仅重验失败/受影响断言，并在四个路由检查间复用同一不变快照。原始失败、超时和通过日志均保留。

Ruff、差异空白检查、恢复包严格类型检查通过。初次失败的 9 项受影响契约均已完成定向闭环，未将超时运行整体计为通过。完整验证清单以 VALIDATION.json 为准。未运行全仓本地测试、全量 Absolute、全场景经济矩阵或新产业周期 holdout。远端大矩阵排队不计通过；实际仓库保护及最终状态在合并前再次读取。

## 经济判断与剩余问题

B 状态：**没有充分依据实施**，不是经济候选失败，更不是经济目标已完成。未发现已被证据确认且可直接修复的新经济错误，也未发现本次恢复入口中身份完整、可比为正、尚未纳入的实现。PR76 的 H1/H2 与 PR77 的 C1/C2/C3 仍保留原失败结论；R26 已在 main，未重复宣称恢复能力。

继续做经济修改需要新的、可比的组合级机制证据：同时解释被阻止的机会与原约束避免的损失，并证明不是历史赢家筛选或单次机会替代。当前没有依据再指定一个策略候选。长期趋势参与、成本后收益和跨场景稳定性目标仍未由本轮取得新进展。

## 保存与恢复

工作 PR：#78，分支 codex/recovery-implementation-convergence；最终合并状态以 GitHub 读回为准。GitHub 保存生产改动、工具、合同、报告与回执。原生 Git 无写认证，连接器构造相同 tree 发布，并用原生 fetch 回读核验。

完整分配输入原件 1,956,329,361 字节，分为 10 片；INPUTS_REMOTE_RECEIPT.json 包含文件身份、顺序、offset、长度和哈希。已回读全部分片，与本地原件逐字节比较并核验重建 SHA-256。不是仅保存摘要。其他原生结果、通过/失败/超时日志、源代码 bundle 与恢复说明保存在 FINAL_REMOTE_RECEIPT.json 指向的完整原件包。

恢复时先读取当前 main AGENTS.md、PR78 和对应回执；下载分片按顺序拼接，核对原件长度和 SHA-256 后，使用 behavior_check.py 的 compare 子命令读取。pickle 仅用于这里已验证身份的自有测试夹具，不接受未知来源输入。源码 bundle 以基线 main 为前置提交，可恢复本地生产者 SHA；不要将等价远端提交冒充本地生产者。
