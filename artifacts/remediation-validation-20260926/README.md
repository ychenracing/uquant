# PR #92 后续验证（2026-09-26，Asia/Shanghai）

## 已验证

- `3271d27a1ea541b3f9847f9ec5b8ac8899532858`：真实公司行动远端运行
  `36156642533`，artifact `10873604760`。`real-company-actions.json` 为原样结果；
  `real-artifact-receipt.json` 记录原始 ZIP 的字节数和 SHA-256，下载后已校验。
  新 baostock 快照含 4 次真实分红，其中 2 次转增。使用研究库存验证股份、成本、现金、
  理论除权净值、幂等和持久化读回；不代表真实券商账户或全部税务身份。
- 同 SHA / Python 3.12.13 / frozen 数据 / 全部 34 股，2023-01-03 至 2026-06-30：
  策略 29.5525×，最大回撤 26.6430%，40 个成交账户订单。
  完整回放原始 JSON 压缩后 258001 字节，SHA-256 见 `relative-full34.json`。
  初次提取误读不存在的 `metrics` 容器，得到空摘要；随后从已保存的原始顶层字段提取，
  未重跑或改动回放。原始文件读回与内存输出完全一致。

## 复用范围

34 股参考策略来自 `artifacts/remediation-20260925/relative_references_universe34.json`，
保留原生产者身份。REF0a 14.4368× / 回撤42.0501%；REF3 31.2458× / 回撤52.1345%。
均为条件历史回放，固定池选择偏差未解决；未执行点时股票池研究。

## 失败与修正

- `3271d27` Extended Performance run `36157128466` 失败。任务 `108144300990`
  因浅克隆缺少不可变证据提交 `7fcf9562`；修正为完整 Git 历史。
- 同 run 任务 `108144301278`：`promotion value must be numeric: result.calmar`。
  生产指标在零回撤路径明确返回 null，报告器却强制有限数值。保留 null，仅在
  max_drawdown <= 1e-12 时允许；有回撤的空值仍拒绝，不补零/无穷、不变更经济门槛。
  回归测试要求零交易路径仍完整输出45单元并因收益不足失败；原始单元和命令日志始终上传。
- `f623a88` Absolute run `36155843028` 恢复分片失败，实际 PR 合并源
  `dc9149cc371fbcee3449497a4a494ab71e31679f`，tree `6b71f30c23a89d427276903bbe68e5577360c3a3`。
  诊断为订单状态/事件组合不合法。生产执行器新增 `AUCTION_LIMIT_NOT_REACHED`，
  严格读回遗漏；仅允许 OPEN/PARTIALLY_FILLED，仍拒绝 SUBMITTED/终态。
  同源 champion 回放26.7586× / 回撤26.6668%，单个数值不代表完整合同通过。
- 初次工作流合同测试命令误写文件路径而未收集测试，记录保留，使用真实路径重试。
- 用户已明确授权取消旧版任务，取消不会删除已有日志和产物；取消不得记为通过。

本目录不覆盖既有证据。完整矩阵及 required checks 的最终结果、SHA、run ID 在 PR #92
如实更新；尚未出齐时不宣称15×/23.284×完整验收通过。配股不支持、三机制保留的结论
见 PROJECT_STATE.md / docs/OPERATIONS.md。
