# 2026-09-14 decision-core rescue

Authority: `UQUANT_PR67_DECISION_CORE_RESCUE_PROMPT_20260914.md`, explicitly activated by the user for PR #67. The frozen `joint-allocation` evidence remains unchanged. Starting remote producer is `076fc3b7ed6535222f6354278cf5a2049b3cd9f1`, tree `599f2c184b1708e751e56b5df220de53683888fd`; its production source is `bbaf58d3c403f79bd96f20bddc1ff849d5c7f960`.

## H1 — bounded ranked ordinary seed cohort (registered before replay)

Mechanism: when legitimately eligible fresh ordinary names have no confirmed mature-cycle sizing, admit a ranked seed cohort rather than independently granting every name a full 20% seed or concentrating the entire seed in one immature name. Reuse the existing leader score only as relative allocation utility, the existing `core_admission_weight` as the batch budget, and `min_trade_weight` to bound cohort size. Allocate the batch score-proportionally; do not alter qualification, risk, concentration, settlement, mature-cycle sizing, or later holding/exit rules.

Decisions this can change:

- E bull, 2025-02-07: three READY mature ordinary names currently each request 20%; H1 makes their combined seed request 20%. Later newly eligible batches remain eligible under the same causal rules.
- E second half, 2024-08-01: two independently qualified immature names currently compete for one 20% slot; H1 admits both within one 20% ranked seed, avoiding a one-name portfolio choice without hindsight selection.

Expected benefit and tradeoff: reduce dependence on one early choice and reduce unconfirmed pre-damage exposure while retaining lawful participation. The tradeoff is less capital in an immediate winner before mature-cycle confirmation and potentially more small orders/costs. A is not expected to change because its May actions belong to `RECOVERY_COHORT`.

Falsification: reject H1 if it does not change both identified decisions, if E's relevant loss/freeze and acute deficit remain materially unresolved, or if directly affected fixed-G/key protections materially deteriorate. A separate authorized recovery-allocation hypothesis will be evaluated from A's native May account only after H1's effect is measured.

H1 result: rejected. Diagnostic source patch `f5bbab65ea16e6d6dd96c3459e2ed53937db27df33ac5e4984c24d3e2f3c1c6e` changed both intended actions. E bull improved only from 2.0787587598x to 2.2203398892x, still failed the 13.3598068604x floor, retained the 2025-02-28 RISK_OFF liquidation/freeze, and worsened drawdown to 19.4154978612%. E H2 admitted both August 1 names at 9.5882%/10.4118%, but acute-prefix wealth fell to 0.9678968245x (about -3.2103%), worse than S. This disproves both sufficiency and the lower-ranked-peer diversification hypothesis. Raw diagnostic traces are `H1-e-bull.json` and `H1-e-h2-acute.json`; they use uv 0.12.11 and are not canonical acceptance.

## H2 — qualification-strength-aligned deployment (registered before replay)

Mechanism: keep every existing READY decision intact, but require the existing persistent mature-market confirmation (or an armed mature cycle) before a local `ORDINARY_CORE` certificate receives capital. Conversely, permit an independently qualified ordinary certificate to receive its existing bounded seed during NORMAL-risk `CHOPPY`, while strategic admission remains closed there. This uses existing certificate confirmation, opportunity, and persistent-market inputs; it changes deployment discretion, not eligibility thresholds or strategic authority.

Decisions this can change:

- E bull: the weak two-session local market opening can no longer deploy several 20% positions before broader persistence is observed; READY remains visible with an explicit deployment-pending reason.
- E H2: the repeatedly READY `sz300604`/`sz300666` independent certificates on July 11-15 become actionable before the August 1 losing choice, without selecting them from future returns.

Expected benefit and tradeoff: avoid premature local-maturity capital while allowing stronger independent proof to act in a range-bound market. It may delay trend winners or deploy into a false CHOPPY reversal. Falsify if the intended actions do not change, E bull retains its dominant failure, E H2 acute remains below its floor, or the first directly affected protections regress materially.

H2 result: rejected as a production candidate. The intended decisions changed and the focused suite passed 35 tests, but the account-path repair was immaterial. E bull reached only 2.1137324649x with 20.8077377615% maximum drawdown and 26 orders, versus S at 2.0787587598x and the unchanged 13.3598068604x floor. E H2 acted on the July 11 independently qualified `sz300666` certificate and improved the acute prefix from about -2.8554% to -1.8835%, but remained about 6.4943 percentage points below the +4.6108107936% floor. Raw diagnostic traces are `H2-e-bull.json` and `H2-e-h2-acute.json`; source patch `388b9275f65e496f2eee77e9a2ba6e0abc3dc6f53de072d7e24c45c3581e8396`, source projection `f3f838b10249f1d04f60448be172890461d9473cff081313b34beb4f1ddbdb27`, uv 0.12.11. This rejects persistent-market confirmation as sufficient deployment proof and rejects the July independent entry as an acute-window repair, while retaining both observations as causal evidence.

## H3 — armed-cycle custody for local ordinary eligibility (registered before replay)

Mechanism: retain local `ORDINARY_CORE` certificates as READY, but let them receive capital only after the existing mature leader cycle is armed. Persistent-maturity evidence remains observable but no longer independently releases shared capital. Independently qualified `FULL_COHORT`, `STRONG_PAIR`, and `ABSOLUTE_SINGLE` certificates keep H2's bounded NORMAL/CHOPPY access. This changes deployment custody only; it adds no threshold, symbol rule, date rule, risk exception, or alternate engine.

Decision this can change: in E bull, local certificates received three 20% requests on February 12 and two more on February 27 even though the mature cycle was not armed. The resulting book suffered a legitimate one-session acute evacuation on February 28 and stayed under the sector guard until June. The frozen historical champion trace recovered from the repository's own phase-one bundle shows the economically relevant alternative path: an empty settled book used the existing April 3 tactical recovery signal, then the confirmed May recovery cohort captured the dominant optical compounding. H3 tests whether present repaired code can lawfully recover that decision path without restoring repeated same-session recovery counting.

Expected benefit and tradeoff: prevent slowly accumulated local maturity from consuming capital before a genuinely synchronized leader cycle, preserving cash for an independently qualified or recovery-owned opportunity. The tradeoff is missed participation when a gradual trend succeeds without arming the mature cycle. H3 targets E bull's dominant missed-compounding failure; H2 already shows it cannot by itself repair E's acute H2 cell, so that cell remains a separate required decision after this discriminating replay.

Falsification: reject H3 if any local-only ordinary order is funded before the February damage, if the existing April recovery path remains inaccessible, if E bull still misses its frozen wealth or drawdown gate, or if directly affected fixed-G/key protections materially regress. Do not alter the sector guard, recovery clocks, tactical thresholds, or strategic ownership to force the result.

H3 result: rejected as an integrated candidate, while retaining its local-custody mechanism as supported evidence. The focused suite passed 36 tests. E bull followed the intended January tactical exit, April tactical recovery, and May recovery-cohort path and passed at 13.5541299857x, 17.7855811578% maximum drawdown, and 13 orders. D passed at 8.8774550752x and 24.2806120730% drawdown. A was unchanged and failed narrowly at 11.6554669082x versus its 11.7313165203x floor. H2's independently qualified CHOPPY exception also produced a first-divergence warning in a fast all-role removal proxy by buying `sz300223` on 2023-01-17, before the January 19 strategic cohort. Readback then established that this proxy fixed future-listed members in the role set instead of applying the native point-in-time removal transport, so its removal metrics are explicitly inadmissible and retained only as a method-error record. H3 is nevertheless rejected because A and E H2 remain known failures; its E-bull result cannot be spliced with I's removal evidence. Raw diagnostic traces are `H3-e-bull.json`, `H3-a-bull.json`, `H3-d-bull.json`, `H3-remove308.json`, and `H3-remove502.json`; source patch `16a298df947e49c532a00aac90705b8db6d4c200276c9c150c71d4bf63edc86e`, source projection `1cb037cc7d577a0ddb63dfde2ca7124c54174411e8de2322c4beadbb2e4ecfbd`, uv 0.12.11.

## H4 — armed-cycle custody with the frozen opportunity gate (registered before replay)

Mechanism: keep H3's evidence-supported rule that a local `ORDINARY_CORE` certificate remains READY but receives capital only after the existing leader cycle is armed. Remove H2's rejected independent-certificate CHOPPY exception and restore the original `_core_opportunity_open` boundary for every ordinary admission. No qualification, risk, settlement, strategic authority, size, threshold, date, or symbol rule changes.

Decisions this can change: E bull retains H3's prevention of the premature February local cohort, while remove-502 can preserve the January 19 strategic authorization instead of being front-run by an ordinary January 17 order. E H2 is expected to revert to S because the July CHOPPY action was already disproved as an acute repair. A remains a separate recovery-allocation blocker.

Expected benefit and tradeoff: isolate the E-bull deployment-custody repair from the disproved CHOPPY mechanism and preserve strategic cohort precedence in removal scenarios. The tradeoff remains missed ordinary participation when local maturity never develops into an armed cycle. Falsify if E bull loses its gate, remove-502 remains materially below fixed G, or the intended first-divergence restoration does not occur.

H4 result: rejected as an integrated candidate. The focused suite passed 36 tests and E bull retained H3's pass exactly at 13.5541299857x, 17.7855811578% maximum drawdown, and 13 orders. E H2 reverted to S's lawful path and therefore retained the known acute failure of about -2.8554%. The patch was checkpointed as `73d9adc` so the native Absolute removal transport could bind physical source bytes without bypassing the clean-source guard. Native point-in-time remove-502 then completed with two real epochs and two distinct owners, but final wealth was only 1.9140110854x with 24.0446184928% drawdown and 14 orders, versus fixed G at 2.3851418754x. That is 80.2472% of G and fails the unchanged 95% wealth-retention floor. `H4-native-remove502.json.gz` is the native diagnostic seal; `H4-remove502.json` is retained only as the explicitly inadmissible fixed-future-membership method-error trace.

## H5 — flat-book custody, deployed-book continuity (registered before implementation)

Mechanism: apply H4's armed-cycle requirement only to the first local `ORDINARY_CORE` deployment when the common capital book has no live filled ordinary holding. Once real ordinary capital is already deployed, permit another READY local certificate through the original budget, position, concentration, cash, risk, opportunity, and settlement checks. A pending order is not treated as deployed capital. Independently qualified certificates and strategic/recovery ownership remain unchanged. This uses existing filled-book state and introduces no threshold, timer, symbol, date, feed, or exception to frozen risk.

Decisions this can change: E bull begins February with a flat settled book, so its premature local cohort remains deployment-pending and preserves H3's recovery path. In native remove-502, the missing March 27 `sz300308` request occurs alongside real `sh688256` and `sz300394` ordinary holdings; H5 restores that deployed-book allocation without reopening first deployment. A and E H2 remain separate blockers.

Expected benefit and tradeoff: preserve a successful existing ordinary book's ability to allocate residual cash while reserving first-deployment custody for stronger cycle evidence. The tradeoff is that a weak incumbent can sponsor another local allocation, but all pre-existing budget, slot, concentration, deterioration, and settlement limits still apply. Falsify if E bull changes from H4, native remove-502 remains below its fixed-G protection, or a flat-book local certificate receives capital without an armed cycle.

H5 result: rejected as an integrated candidate. The focused suite passed 37 tests, including a red-to-green test that distinguishes a flat book from a filled ordinary book. E bull remained byte-path equivalent in its account orders and retained H4's exact passing metrics: 13.5541299857x final wealth, 17.7855811578% maximum drawdown, and 13 orders. Native point-in-time remove-502 exactly restored fixed G at 2.3851418754x final wealth, 23.8124234993% drawdown, 20 orders, two realized strategic epochs, and two distinct owners. Native remove-308 then falsified H5: final wealth fell to 0.8777724074x from G's 1.1550544605x, with 23.0050912167% drawdown and 11 rather than 16 orders. The canonical G `loo-f` artifact from run `34806466254`, attempt 1, was downloaded with matching archive SHA-256 `08d05fd3d53e5870c78659edca6aee925411c67215a5983238bed91eedbabaf9`. It showed the missing path: after the final strategic sell filled on March 27, G immediately allocated 20% each to three persistent-mature local cores, added a fourth on March 28 and residual capital on April 4. H5 instead waited for the April 7 armed cycle and concentrated 40% each in two names immediately before a legitimate crisis loss. Diagnostic seals are `H5-native-remove502.json.gz` and `H5-native-remove308.json.gz`. The source patch for H5's E replay was `02cddbe839893e9a402e992539b7baad61200a884d374484b9009f7f0d398dc8`, source projection `8078d05d5f531c88af83fe198ebce390f72c604e174f649883cc90f1bc89899f`, based on checkpoint `73d9adc530b551fd3a0bc88690f0ae2dca2c3a70`.

## H6 — settled-owner rotation continuity (registered before implementation)

Mechanism: retain H5's first-deployment custody for a genuinely initial flat account and its continuity for live filled ordinary capital. Add one causal release path when a real strategic ownership epoch has completed on the current session, its sells are actually settled, and the existing five-session persistent-maturity observation is open. In that state, READY local `ORDINARY_CORE` certificates may use the original ordinary budget, slots, caps, concentration, cash, opportunity, risk, and settlement checks. A historical epoch by itself is insufficient; the completed-exit session and persistent local evidence must coincide. No score cutoff, new threshold, date/symbol rule, anticipated proceeds, strategic-right change, or risk exception is added.

Decisions this can change: native remove-308 can rotate actually settled March 27 strategic capital into its already READY persistent local cohort instead of waiting until April 7 and concentrating mature-cycle capital immediately before the crisis. Native remove-502 continues through H5's live-ordinary path. E bull has no completed strategic ownership epoch on its February initial flat-book decisions, so its premature cohort remains deployment-pending and its H4/H5 passing recovery path should remain unchanged.

Expected benefit and tradeoff: preserve capital continuity across a completed owner lifecycle without treating every flat account as proven. This can still rotate into a false persistent trend, but only after realized strategic ownership and under every original capital/risk safeguard. Falsify if the new sentinel does not release only the coincident completed-owner/persistent state, if E bull changes from H5, if remove-308 fails its unchanged G protection, or if remove-502 regresses.

H6 result: retained as the current partial candidate. Its new sentinel failed before implementation and passed afterward; the focused ordinary/capital/strategic suite passed 51 tests. E bull stayed exactly at 13.5541299857x final wealth, 17.7855811578% drawdown, and 13 orders. Native remove-502 stayed exactly at G's 2.3851418754x wealth, 23.8124234993% drawdown, 20 orders, two epochs, and two owners. Native remove-308 restored the 16-order structure and reached 1.1190839397x wealth, 22.8889194976% drawdown, two epochs, and two owners. That retains 96.8858% of G's 1.1550544605x wealth and clears the unchanged 95% floor. The stricter completed-epoch condition releases capital on March 28 rather than G's March 27 post-fill decision; it therefore selects `sz300502` before adding `sz300394`, rather than claiming byte-path identity. Diagnostic evidence is `H6-e-bull.json`, `H6-native-remove308.json.gz`, and `H6-native-remove502.json.gz`, bound to checkpoint `050b50d1b1abe5a74571668e6bc8961e01bc64bb`.

### E H2 acute authorized-scope bound

The retained H6 mechanisms do not change E H2, so its frozen August 1 through September 2 return remains -2.8553764161% versus the +4.6108107936% floor. Removing the August 1 ordinary selection entirely is an admissible upper bound for that decision layer because both legitimately READY names (`sh688361`, `sh688146`) lose over every executable next-session-to-prefix path. Adding back `sh688361`'s exact -32,604.8041 realized contribution and costs raises terminal prefix equity only from 1,931,565.0990 to 1,964,169.9031 against starting equity 1,988,339.6813, or -1.2155759139%. H2's earlier independently qualified `sz300604` and `sz300666` also decline from August 1 to every executable later prefix exit. No other READY ordinary opportunity exists before September 2. Therefore ordinary selection, sizing, or exit can reduce loss but cannot meet the positive floor. The historical positive tactical path is unavailable while the fixed strategic owner and its August 6-12 sell obligations remain live. Passing this cell would require changing a frozen strategic/obligation rule, admitting an otherwise ineligible opportunity, or changing the gate; none is authorized, so the acute cell remains NOT_MET rather than being weakened or hidden.

## H7 — leadership-weighted recovery expansion (registered before implementation)

Mechanism: when a real existing recovery member expands into two or more confirmed secondaries, allocate the already-fixed secondary target budget in proportion to the secondaries' current positive finite leader scores. Keep the incumbent target, cohort gross, candidate eligibility, member count, max-symbol/concentration/risk caps, next-session execution, and settlement unchanged. Invalid scores retain the existing equal fallback. This replaces equal secondary target rights followed by score weighting only when fresh cash happens to be scarce; it does not calibrate scores into expected returns or add a parameter.

Decision this can change: A's May 8 expansion currently assigns equal 16% target caps to `sz300394` and `sz300502`, then scarce cash gives the higher-score `sz300502` its cap and the lower-score peer the residual. H7 lets the target split itself express the already-observed 0.4456 versus 0.1598 leadership difference while preserving the existing 92% recovery gross and the incumbent's filled price drift. The resulting anchor rights also govern later legitimate restoration, so the test includes that full tradeoff rather than evaluating May in isolation.

Expected benefit and tradeoff: direct more bounded recovery capital to the stronger confirmed peer without selecting by future return. The tradeoff is higher member concentration and persistence of that split if relative leadership later reverses. Falsify if the rule is order-dependent, changes the total target budget, moves the incumbent target, fails A's frozen floor, or creates a directly affected protection regression.

H7 result: rejected and removed from the production candidate. The targeted sentinel failed before implementation and passed after implementation, and the directly affected recovery tests remained green. A improved from 11.6554669082x to 11.6952946097x final wealth, with 16.1287973285% maximum drawdown and 12 orders, but still missed the unchanged 11.7313165203x wealth floor by 0.0360219106x. Because the hypothesis was preregistered to fail on that cell, its target-right change and sentinel were reverted while this result and `H7-a-bull.json` were retained as negative evidence. The production candidate therefore returns to H6.

## H8 — leadership-sized entry, fixed restoration rights (registered before implementation)

Mechanism: when a real existing recovery member expands into two or more confirmed secondaries, use the secondaries' current positive finite leader scores to divide only the already-fixed secondary budget for the new entry. Preserve the existing equal secondary recovery-owner targets as the durable rights used after any legitimate risk exit. Invalid scores retain the equal entry fallback. The incumbent target, total cohort gross, eligibility, member count, risk and concentration caps, execution timing, settlement, and all exit/restoration triggers remain unchanged.

Decision this can change: H7 showed that shifting A's May 8 fresh expansion toward the stronger current peer improves the full path, but it also persisted those May score proportions into the October 21 restoration, when the relative opportunity had changed. H8 keeps the causal May entry sizing while the existing equal owner rights govern October restoration. It therefore coordinates fresh selection utility with established recovery protection instead of treating an old score as a permanent entitlement.

Expected benefit and tradeoff: retain the measured pre-crisis benefit of allocating scarce new cash toward current leadership without allowing that transient ranking to rewrite later risk-restoration rights. The tradeoff is that actual holdings can temporarily differ from their durable restoration targets, as they already do through price drift. Falsify if entry or restoration becomes order-dependent, the target budget or incumbent moves, invalid scores lose the equal fallback, A misses its frozen floor, or a directly affected protection regresses.

H8 implementation note: the first implementation correctly separated A's entry from its owner rights, but its funding branch also treated D's already-persisted unequal recovery rights as transient. That spillover produced 8.6948140320x D wealth, below D's 8.8195014189x floor. `H8-v1-d-bull.json` preserves the failed result. A new sentinel reproduced the error before the branch was limited to targets that actually differ from `account.anchor_weights`; the corrected sentinel and the recovery budget suite then passed. `H8-v1-e-h2-acute.json` is also method-invalid because that exploratory command initialized on August 1 rather than the required July 1 warmup. Both are retained as error history, not candidate evidence.

H8 result: retained as the strongest partial candidate, but not accepted. On the corrected committed source, A passes at 11.8706585867x final wealth, 16.0048175923% maximum drawdown, and 12 orders; the May 8 entries are 7.3009050705% `sz300394` and 20.3567091071% `sz300502`, while the October 21 restoration retains the original 60%/16%/16% owner rights. D returns exactly to the protected H3 path and passes at 8.8774550752x wealth, 24.2806120730% drawdown, and 18 orders. E bull passes at 13.8210227236x wealth, 17.2530367369% drawdown, and 13 orders. Native remove-308 passes at 1.1190839397x wealth, 22.8889194976% drawdown, 16 orders, two epochs, and two owners; native remove-502 remains exact to fixed G at 2.3851418754x wealth, 23.8124234993% drawdown, 20 orders, two epochs, and two owners.

The same H8 source leaves E second half exactly at 1.3955925424x wealth, 8.8492695229% drawdown, and 10 orders. Its August 1 through September 2 equity remains 1,988,339.6813 to 1,931,565.0990, or -2.8553764161%, versus the unchanged +4.6108107936% acute floor. This is the authorized-scope bound recorded above: eliminating the only losing ordinary fill still reaches merely -1.2155759139%, and no other eligible executable opportunity can supply the missing positive return while the fixed strategic owner and settlement obligations remain. H8 is therefore NOT_MET overall. The 34-LOO/final Absolute matrix was not launched for a candidate already known to fail a required cell, and no old candidate's evidence is spliced into H8.

All H1-H8 replays in this directory are local diagnostics under Python 3.12.13 and uv 0.12.11 unless a file is explicitly named `native`; none is canonical acceptance. The native diagnostics use the frozen point-in-time Absolute transport but remain noncanonical because the documented runtime requires uv 0.11.33. `SHA256SUMS.json` and `verify.py` seal the saved bytes and recheck the stated key metrics and method-validity labels.
