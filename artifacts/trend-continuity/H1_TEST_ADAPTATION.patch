diff --git a/tests/test_slow_structural_exit.py b/tests/test_slow_structural_exit.py
index aacf2a3..a25b1aa 100644
--- a/tests/test_slow_structural_exit.py
+++ b/tests/test_slow_structural_exit.py
@@ -32,6 +32,7 @@ def observe(policy, account, symbol, frame, leader, date):
 
 def test_confirmed_structure_loss_exits_despite_pre_entry_rally():
     policy, account, symbol, frame, leader = setup_case()
+    frame["ma60"] = 100.
     assert frame.iloc[-1].ret20 > 0
     assert frame.iloc[-1].close < account.positions[symbol].avg_cost
     outcomes = [observe(policy, account, symbol, frame, leader, date) for date in frame.index[-3:]]
@@ -50,6 +51,14 @@ def test_mature_and_protected_winner_controls_remain():
 
 def test_repeated_same_session_does_not_accelerate_confirmation():
     policy, account, symbol, frame, leader = setup_case()
+    frame["ma60"] = 100.
     date = frame.index[-3]
     assert not any(observe(policy, account, symbol, frame, leader, date) for _ in range(3))
     assert account.replacement_tenure[f"lifecycle_exit:{symbol}"] == 1
+
+
+def test_intact_medium_structure_is_retained_without_prior_profit():
+    policy, account, symbol, frame, leader = setup_case()
+    frame["ma60"] = 85.
+    assert account.positions[symbol].highest_close == account.positions[symbol].avg_cost
+    assert not any(observe(policy, account, symbol, frame, leader, date) for date in frame.index[-3:])
diff --git a/tests/test_strategic_core_structural_exit.py b/tests/test_strategic_core_structural_exit.py
index a2ef0db..a521ef8 100644
--- a/tests/test_strategic_core_structural_exit.py
+++ b/tests/test_strategic_core_structural_exit.py
@@ -22,7 +22,7 @@ def _aged_core(*, partial=False):
 
 def _damage(panel, leaders, dates):
     frame = panel[OWNER]
-    frame.loc[dates, "ma20"] = frame.loc[dates, "close"] * 1.10
+    frame.loc[dates, "ma60"] = frame.loc[dates, "close"] * 1.10
     frame.loc[dates, "ret20"] = -.09
     return {symbol: replace(leader, mature=False) for symbol, leader in leaders.items()}
 
@@ -89,7 +89,7 @@ def test_core_exit_preserves_existing_conjunction_and_entry_completion(missing):
     if missing == "maturity":
         damaged = leaders
     elif missing == "price":
-        panel[OWNER].loc[dates, "ma20"] = panel[OWNER].loc[dates, "close"] * .95
+        panel[OWNER].loc[dates, "ma60"] = panel[OWNER].loc[dates, "close"] * .95
     for index, date in enumerate(dates[:4]):
         current = leaders if missing == "interrupted" and index == 1 else damaged
         assert not _decide_and_submit(allocator, account, date, panel, current, roles)
