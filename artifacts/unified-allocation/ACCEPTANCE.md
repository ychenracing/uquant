# 组合分配替换：事前合同

本合同仅适用于本轮；不改写 PR76/77 失败结论。所有窗口为研究数据。候选运行前登记。

保留信号、资格确认、目标仓位公式、现有持仓生命周期/退出、风险限额、战略所有权、待成交及风险恢复权。将新恢复/普通请求共用 LeaderScore.score 降序、代码同分序的资金流程；恢复候选内部深跌筛选仍保留。取消恢复组硬锁定、追加窗口否决、同业请求独立比例资金分配和整段普通准入互斥。保留持仓优先和真实待成交承诺，不主动换出健康持仓。不得把基线账户/风险/订单注入候选。

A：每场景终值保留95%，六个普通成本场景几何保留98%；净减至少60生产行、至少3条活跃分配规则，无新增持久状态。用户允许小幅收益换简单；5%局部/2%整体避免以删代码掩盖大损失。
B：每场景终值保留90%，六场景几何提高2%，至少两场景提高1%；整体复杂度不增加。容忍较大的局部取舍须由组合改善抵偿。
共同：最大回撤<=30%且增加<=1百分点；订单<=max(B+2,1.15B)，操作日<=max(B+3,1.15B)，换手和归一成本<=1.15B。15%执行预算允许实际机会竞争，仍限制维护/操作负担。双成本两个场景分别满足路线终值底线和共同约束。数值均程序判定，不看结果修改。

范围：2024、全期、剔除300308、剔除三光模块先行；再2025-04账户、剔除300502、全期及2024双成本，共8对。结束日2026-08-05。统一冻结34池、数据、起始现金、成交、成本。当前运行时Python3.12.13/uv0.12.15与旧回放不同，因此补跑配对基线，不冒用旧身份。numpy/pandas及依赖按uv.lock。

一次有证据修订上限；实现错误可修复重验。短期检查只检验正确性，不替代全路径经济结果。保全失败。验收通过才整合并删除退役分支，失败保留研究补丁而不合并。
