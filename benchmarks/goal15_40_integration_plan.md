# Latest user authorization and fixed candidate integration

2026-09-10: user explicitly accepts15-fold wealth in place of23.28, never below15; at most40orders. Principal full/champion continuous floors and current Absolute champion use15 exactly, with no90% multiplication. All absolute per-account replay order limits use40, including current champion and shorter windows. Existing drawdown, cost, turnover, participation and other obligations remain. Original frozen contracts and literal failures retained; v4 effective judgment is distinct. No main merge.

Choose the already implemented slow-structural-exit candidate. Integrate on currentPR infrastructure by restoring the existing maturity guard and removing only the velocity condition. Retain prior neutral cache/discovery/data fixes and retired pullback admission, rather than restoring stale infrastructure. These integration differences require native equivalence checks before reusing prior candidate outcomes. No new stock exceptions, periods, weights or entry/recovery changes.

The old C maturity-veto-removal hypothesis is retired. Its tests and failures remain recoverable at387033750a32e55579e9fd75b6392e63a4d05fef and saved evidence. Current tests assert the selected main-policy maturity behavior; full native partial/restart/identity exit proof is extended to positive ret20. This intentionally replaces obsolete behavioral assertions, not numerical acceptance or execution safeguards. New15/40 and slow-exit tests first fail6/7 on oldproducer, then pass7/7.

First run affected tests/static checks; freeze producer. Native repaired strict and frozen no-optical H1 checks validate integration and entry/holding effects. Then necessary frozen principal/performance/robustness/Absolute/ownership gates on a stable candidate. Reuse only identity-compatible evidence, with original failures retained. Failure selects a concrete repair/causal next step, no parameter search or new arbitrary cycle.

Source binding repair: Absolute still pins the earlier d8b104b3 candidate, so the current producer is rejected before policy evaluation. Update only the current candidate source field and its dependent seal/compiled constants to the measured73969473 fingerprint. The original frozen17ce contract, all policy/input/baseline fields, and stale-identity failure log remain unchanged. This follows the existing frozen-policy projection and exact-source check, with no dynamic self-approval. The prerequisite d7fd baseline blobs were restored through explicit repository reads and independently verified by Git blob hashes. All local checks disable implicit lazy fetch.
