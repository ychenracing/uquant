# Repair evidence tooling without changing the economic policy

The native recovery candidate remains NOT_MET. Its fixed bull failure and the
historical permission ablation are retained. This work addresses actionable CI
defects in newly added research/evidence tools, not profitability.

- Replace research evidence `assert` checks with explicit exceptions so Python
  optimization cannot silently disable source, accounting or data validation.
- Retire the completed historical9bb-only replay/trace and empty-b43-prefix
  diagnostic from the active research package. Their exact scripts and results
  remain in prior Git commits and saved raw evidence. They cannot run against
  the current producer and are not production dependencies. Update only the
  active full-package path inventory, never frozen evidence or thresholds.
- Give the reusable fixed-unit diagnostic one public function in its actual
  promotion owner; preserve every replay, cache, bound, original/effective gate
  and explicit non-acceptance label. Full promotion still requires the full
  profile. Use the existing public runtime/compact interfaces where possible.
- Expose the already-shared research-data root validator under its real public
  name. No duplicate validation or bypass.
- Replace unrestricted URL openers in the two public-input acquisition scripts
  with HTTPS-only stdlib transport. Keep their approved sources, dates and raw
  evidence; no acquisition is required for this repair.

Validate the affected existing research tests, actual security differential,
public-interface/import checks, Ruff and strict typing. Preserve first failures.
Do not expand architecture allowlists, remove economic gates, substitute runtime
version strings, or call this work a strategy improvement. Remaining historical
architecture transport/Ownership/Absolute failures require their own diagnosis.
