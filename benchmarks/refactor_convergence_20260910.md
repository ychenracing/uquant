# 改造收敛结论（2026-09-10）

G 两个预先指定的冲突窗口均已完成，原始账本、归因、时间顺序、源代码、配置、数据、股票池和运行环境核验通过；策略筛选结论 FAIL，不推广、不再微调这个过滤条件。

| no_optical 窗口 | 最终财富（含本金） | 最大回撤 | 订单 | 当前判定 |
|---|---:|---:|---:|---|
| 2024 下半年 | 0.963584316 | 4.1565% | 6 | 财富低于 1.544453477，FAIL |
| 2025-2026 后期 | 5.792557953 | 20.2727% | 30 | PASS |

原始严格判定仍保留：后期原始订单上限失败。小幅容差保持既有 v6，不叠加放宽；H2 财富距离不是小误差。两个回放进程正常退出 0，没有未完成的 G 回放。未跑 G 全矩阵、压力测试或完整 L4；E 的 Absolute 绑定不能充当 G 证据。

## 距离目标

主策略历史财富至少 15 倍、订单不超过 40 的核心数值目标已有同一 E 版本的证据：champion 25.3413 倍/14 单，full 28.2118 倍/20 单。整体目标还包含跨窗口稳定性、去除核心标的后的能力、成本与扰动压力、完整工程/L4验收，尚未全部达到。14 项名义比较的通过数量不能当作项目完成百分比，也不能把不同版本的最好成绩拼成一个通过版本。

E 完整名义矩阵 11/14 通过，两个 H2 重大退化；G 进一步过滤虽然降回撤，却失去 H2 收益。因此停止当前整套普通入口/退出替换路线。最接近名义目标的研究基线仍是 a39（13/14），唯一名义失败是 no_optical 后期 1.125312594 对 1.585410050，下限比实际高约 40.89%。这不是只差 1%。a39 仍非已验收生产版本。

## 后续路径与停止条件

1. 回到不可变 a39 对照证据，保留现有资格、恢复和生命周期机制。先定位后期具体资格/执行阻断；不再次做全仓位普通策略替换，不增加日期或股票池例外。
2. 只有发现独立因果依据的局部缺陷才实现一个修正。简单入口捕获后期收益是机制线索，并非已证明可安全隔离的修复；A 持仓恢复方案已失败，不重新包装。当前没有已验证能补齐唯一缺口的修复，不能承诺这条路径必定达标。
3. 修正先过后期、两个 H2 和战略主窗口；重大退化即拒绝，不以调参或累计放宽掩盖。没有可独立解释的修复，就明确报告目标取舍，不自动启动下一轮猜测。
4. 只有稳定候选通过名义验收，才补成本压力、参数邻域、初始条件、最佳贡献者剔除和完整 L4/工程验收，生成真实当前源运行夹具并完成操作交付。修工程绑定本身不产生收益。

独立复核同意以上结论：停止整体替换、保留 a39 为研究基线；未发现可直接采用的已验证修复。未证明目标不可达，也没有保证未来收益。PR 56 保持 Draft，执行头 008e14088276aff6d73448f03249eebbad3bbf8f 未变。

历史计划、失败、原始结果和讨论均保留；当前结果及归档身份见 simple_sector_backdrop_result.json，E 全矩阵见 simple_full_lifecycle_result.json。
