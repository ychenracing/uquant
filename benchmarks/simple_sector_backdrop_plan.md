# One bounded correction to the ordinary fallback

E has viable principal and later results but material H2-2024 regression. Preserve
all results and do not loosen that gap. The next hypothesis preserves original
candidate_entry READY even on an empty ordinary book; strict strategic claims,
current pending FULL formation and independent cash-rearm proof remain mandatory.
Only when strict qualification is not READY and no strategic claims/formingFULL
exist can the already fixed simple stock trend serve as fallback. Require current
finite nonboolean tech_ret120>max(0,broad_ret120), using the unchanged explicit
risk-reference evidence. No date/pool selector, new tuning field, search, MA/
position/capital change, new clock, future data or risk bypass. This is an explicit
new relative-sector scope hypothesis, not a demonstrated economic improvement.

Keep completed FULL whole-group exit, legacy compatibility and missing-frame guard.
First pair: no_optical H2-2024 and no_optical disjoint later, with unchanged current
limits and complete native readback. Only if both pass extend affected final
acceptance. Major failure rejects this hypothesis, not another parameter setting.
Nine new market/stale/strict-priority tests first failed before change, then PASS.
90 relevant tests PASS, Ruff/mypy PASS. Existing simple setup tests now supply the
new market precondition; their original behavior assertions remain unchanged.
Current Absolute binding still names E and will require honest rebinding if G is
viable; original739 successful raw fixture remains stale and must be regenerated
from an actual final candidate runtime, never rewritten with a new source label.

Reviewer found strict READY certificates omit ret120, causing their ranking to
default0 behind simple candidates. New mixed-qualification ranking test first
reproduced002,001,003 instead of003,002,001. All READY candidates now derive the
same causal ret120 from available close history, independent of qualification
payload. Nonfinite ranks sort last internally and are not serialized as evidence.
This repairs the declared common ranking, not a different rank parameter.
