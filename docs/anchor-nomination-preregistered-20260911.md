# Final bounded mechanism: initial anchor nomination continuity

The first membership-permutation candidate has identical completed economics
to Z for offset5/remove_all_three, offset20/remove_all_three and offset20/no_optical.
Even if the fourth target improves it cannot meet two improved target conditions.
Its risk-state correctness fix is real, but no generalization improvement is claimed.
The remaining principal screening jobs continue in their immutable producer.

New causal distinction: initial basket membership churn, not rank permutations.
In Z offset20, the first nomination on 2023-02-07 contains sh688200, sz300666,
sh688256. On all five sessions through February13 they retain confidence1 and
secular scores above the existing .55 threshold, span three industries, and
the account is NORMAL with zero votes. February13's top-ranked basket substitutes
a new stock although the existing nomination remains fully qualified, resetting
the four accumulated confirmations. This is a relative-ranking barrier to
initial risk coverage, not absence of absolute qualifying references.

For an account without an active anchor basket only, continue the pending
nomination while every member still passes the existing selection predicate,
required count and industry coverage. If a member loses eligibility or the
basket becomes incomplete, use the current ranked basket and restart confirmation.
Unhealthy periods still clear confirmation. Existing active baskets retain the
current reanchor policy. No new state, parameter, stock/date-specific condition,
risk allowance, or confirmation-day change.

This is the second and final bounded mechanism. It includes the tested
membership identity correction and uses exactly the same Z target group,
success controls, neighboring conditions, samples and once-only numeric tolerance
in incremental_generalization_plan_20260911.json. Freeze before native replay;
screen the same five economic accounts. Reject if the fixed sample fails; no
further threshold/parameter changes or third strategy candidate.

Expected decision change: initial risk coverage can form after five genuinely
qualified healthy observations despite relative-ranking churn. It may enable
already-qualified entries earlier, and may also change real risk timing; complete
account and risk comparisons remain mandatory. The observation does not promise
higher wealth. Final acceptance remains required before merge.
