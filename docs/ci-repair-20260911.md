# 已知 Actions 工程失败修复 — 2026-09-11

本次修复来自 main `136374a3d34e39c14c1f673a7b10edd10cb67d1e` 及其等价生产者已结束的失败日志。开始时仍在排队或运行的 GitHub 作业没有重跑、取消或等待；本地验证也设置了不超过 10 分钟的单次上限。

## 已修复的根因

- 12 项 lint：整理导入、删除未使用导入、明确 zip 长度要求及局部变量名称。
- 7 条私有导入违规：研究入口使用现有实现的公开名称；新增名称与原函数为同一对象，未更改验收实现。
- 17 项应用测试失败：对齐已接受的 CAUTION 小仓探测、实际恢复持仓及剩余额度、减仓优先级、配置字段和已有晋级授权。保留冻结、流动性、重复日期、资金上限和未授权路径的拒绝测试。
- 搬迁后的架构测试：变异探针定位到真实 AllocationBook 所有者，显式补齐已接受的来源文件投影，支持 DatetimeIndex 的严格 JSON 序列化，并准确投影 PR58 的锚点记忆修复。当前公共 API 合同与当前实现同步；历史 artifacts 没有改写。
- 8 个复杂度超限函数：抽取重复或完整的校验条件。展开辅助函数后与原实现等价，保留判断顺序、阈值、订单身份和风险条件；没有放宽复杂度预算。
- 历史 Absolute 单元测试：显式使用历史或原生样例对应的测试上下文。正向测试的临时合同仅存在于测试目录，不绑定当前源码，也不替换冻结文件。独立负向测试继续验证真实当前源码不匹配时必须拒绝。

## 边界

真实历史 Absolute/Generalization 合同仍不适用于当前源码；Ownership critical 的收益门槛与 continuity 的真实 epoch 数门槛也仍未达到。这些属于真实策略/合同未通过，工作流及门槛保持启用，未使用 skip、xfail、continue-on-error 或新容差把它们改成成功。因此本次不宣称 GitHub Actions 全绿。

新经济源码指纹为 `f270382d9a4eaa692dd9340eb8e035ecf021478eaafdfedf9e23e7e5560d6e36`。本次生产改动为经等价核验的结构整理；PR58 的 55 账户历史证据保留原始生产者身份，没有重标为新源码的原生运行。已有账户继续遵守既有代码身份迁移流程。

## 已完成的验证

以下测试组有重叠，不相加为总覆盖量。

| 范围 | 结果 |
| --- | --- |
| 应用与 CAUTION 例外测试 | 583 通过 |
| lint 相关测试 | 22 通过 |
| 普通入场 / 再入场测试 | 82 / 111 通过 |
| 正式导入、晋级诊断及覆盖保护 | 27 通过 |
| 严格零私有导入违规门 | 通过 |
| 全部架构治理测试 | 78 通过 |
| 最终源码的账户轨迹、来源与完整 API 测试 | 8 通过 |
| 来源与历史封印负向测试 | 16 通过 |
| Absolute 工作流契约测试 | 4 通过 |
| CLI 退出状态、封印及真实源码拒绝 | 4 通过 |
| Absolute runner、场景及缓存最终复测 | 90 通过 |
| Absolute setup、运行时及历史补充测试 | 22 通过 |
| 十个受影响函数的展开等价核验 | 全部通过 |
| 完整复杂度测试 | 5 通过 |
| 最后一次重复校验整理的再入场回归 | 52 通过 |

本地 Ruff 0.16.1 检查通过；CI 锁定版本仍为 0.16.2，没有改依赖锁。当前环境没有 mypy，本次未宣称本地类型检查通过。Absolute 广泛测试运行在 600 秒结束，退出码 124，此前显示 280 个通过且无失败输出；该运行不计作整组通过。最终复测结果见本次 PR。
