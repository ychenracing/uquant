# U: shared admission after real settlement (pre-implementation)

Parent885180ae3e3fe8219324edeb1d0b7e54f60be8b4/sourceee812. Original frozen cross-AI and Ownership assertions remain unchanged. No parameter search or symbol/date exception.

Two separately testable boundaries:
1. A terminal, zero-real-BUY deployment whose matching epoch and actual BUY ledger are still unsettled cannot arbitrate a fresh ordinary admission before reconciliation. Existing valid pending commitments and real holding management remain. No fixed waiting days; stale no-ledger residue alone is not a blocker; any real peer BUY excludes a zero-fill deployment. Native few-day failed-first fixture is the discriminating test.
2. Every stock receiving its first strategic capital must pass the existing CORE entry predicate using its own genuine current route certificate, or the existing mature/independent confirmation path. Group witnesses remain witnesses; group qualification alone no longer funds an independently unready peer. This is a deliberate policy change, not merely a marketblock refactor. It adds no unconditional maturity, independent five-day clock, generic market veto, new state, parameter, universe role or threshold. Current qualified champion has a genuine certificate despite mature=false; H1/2024 observed STRUCTURE_NOT_REPAIRED; H2/2024 one funded witness lacks its own certificate and is NOT_MATURE. Continuous positive holdings and legitimate risk-compression restoration remain separately authorized and unchanged.

Move existing entry helper implementation to the already shared qualification_candidates leaf to avoid duplicate rules/circular ownership imports. Discovery supplies actual per-stock entry results to initial ownership funding; do not manufacture certificates for all witnesses. Native tests must distinguish certificate-qualified non-mature owner, witness-only member, failed current structure, and retained actual holdings.

Validation sequence: precise new negative/positive nodes → affected owner/entry/settlement modules → three nominal sentinels (champion; no_optical H1_2024; remove_all_three H2_2024), at unchanged contract values. Capture exact immutable source/config/data/lock/runner identities before economics. A failed sentinel is a real failure, not a reason to tune its threshold. Do not rerun prior S/T matrices. Only a stable viable candidate proceeds to all14/64 and full final acceptance.

This mechanism does not claim to resolve the critical first-epoch repair-authorization path, later risk-repair health windows, or every economic failure. Those remain active work, not an approval stop. The contract-revision document is an unapplied alternative, not a requirement to ask the user to continue.
