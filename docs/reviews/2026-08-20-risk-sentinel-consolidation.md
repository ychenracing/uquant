# Phase 8 Risk Sentinel Consolidation Review

## Decision

**PASS — consolidate evidence and reporting only.** Phase 8 starts from
production `main` at
`711af1179aa72ce48ca3a6af58ecddb3a029a7ce`. It does not merge the rejected
Phase 7 branch and does not enable Sentinel causal authority. Production keeps
`risk_sentinel_causal_confirmation_enabled: false`.

The only production-source change is the compact, observational Risk Sentinel
section in `uquant/report.py`. There are no changes to strategy, position
sizing, gross caps, symbol limits, sell authority, account schema, risk state,
targets, execution, or fills.

## Phase 7 recovery inventory

The complete Phase 7 path range
`711af1179aa72ce48ca3a6af58ecddb3a029a7ce..c559c009db309b3815aa8a3df8b59638504acc1a`
was reviewed. All 27 changed paths are classified exactly once in
`artifacts/sentinel/evidence_closure/phase7_recovery_inventory.json` and
`docs/reviews/phase7_artifact_review.md`.

Retained or rewritten:

- the generic order-ledger first-divergence trace;
- the existing-path multi-symbol atomic-rotation regression;
- evidence analysis rewritten as an observational closure tool;
- compact Phase 7 rejection facts and audit contracts.

Discarded:

- exclusive-Freeze authority and its integration wiring;
- candidate configuration and configuration identity;
- candidate state-machine implications and trading paths;
- candidate account code identity and large candidate-only artifacts.

The archived Phase 7 result remains **REJECTED**: its one trusted, non-severe
exclusive event on 2024-06-25 blocked zero actual new-risk actions and produced
zero economic deltas. No Phase 7 Future Holdout Lane or stable tag exists.

## Sentinel Evidence Closure

The point-in-time comparison is generated by
`research/sentinel_evidence_closure.py` and sealed in
`artifacts/sentinel/evidence_closure/evidence_closure.json` through 2026-08-05.
It trusts only `market_velocity`, `breadth_structure`, and
`covariance_stress`; it uses no account history and grants no production
authority.

| Family | Base first date | Sentinel first date | Classification |
|---|---|---|---|
| `market_velocity` | 2014-02-25 | 2014-02-25 | DUPLICATE |
| `breadth_structure` | 2014-02-25 | 2014-02-26 | DUPLICATE |
| `covariance_stress` | 2015-08-18 | 2024-09-30 | DUPLICATE |

Closure totals are 3 duplicate, 0 earlier, 0 incremental, and 0 false-positive
first-Family events. Production opportunity cost is therefore zero.

## Daily Report closure

The ordinary `uquant daily` report now includes exactly one compact section:

```text
## Risk Sentinel
- Mode: FREEZE_ONLY
- Level: CAUTION
- Coverage: READY
- Confidence: 91.0%
- Owner: **NONE**
- Risk Families: breadth_structure, market_velocity
- AI Industry Risk: design, optical
- Conclusion: normal execution; Sentinel remains observational.
```

Owner is one of `NONE`, `BASE_RISK`, `SENTINEL`, `BOTH`, or
`DATA_NOT_READY`. The renderer reads the existing immutable decision summary;
it neither recomputes risk nor emits sell, reduction, symbol, target, or order
advice. `artifacts/sentinel/evidence_closure/daily_report_example.md` contains
the complete sample.

## Exact economic equivalence

`research/committed_economic_equivalence.py` compared clean production main
against the Stage 8 candidate on the authenticated 45-case Phase 1 matrix. The
proof is bound to baseline
`711af1179aa72ce48ca3a6af58ecddb3a029a7ce`, candidate
`85c9f208258bba10a19103c82cacf52ef95fa4c2`, data snapshot
`20260809T094222Z-causal-tech-index-rebase`, and matrix SHA-256
`d3435044bb6d2936d881a4d481e015044239f246493499b6700d6ff01013838b`.

All 45 cases are byte-for-byte equivalent. Baseline and candidate trace SHA-256
are both
`8dab09e7019c1f0c8ea4e21f1f061260a2c4eb25a87abd8d75da73f86cab57f3`.
The sealed exact dimensions are:

- Decision Digest and RiskAssessment control fields;
- target portfolios, orders, and fills;
- economic AccountState fields;
- final wealth, maximum drawdown, and trade count.

Commits after the candidate evidence commit only add or align documentation,
research tooling, artifacts, and tests; they do not change production strategy,
configuration, data, or runtime behavior. The proof is recorded in
`artifacts/sentinel/evidence_closure/economic_equivalence.json`.

## Account code identity migration

An explicit `account-code-migrate --acknowledge-code-change` was performed on a
five-symbol production-main account at 2026-08-05. Schema version remained 5.
The economic-state SHA-256 before and after is
`ddb996e13ef7e1f5689474b02a36934a73da1b2e7abb541b806a07b1b097a449`.
Only `code_hash` and the `code_identity_only` migration event changed; the
economic payload is exactly equal. The sealed record is
`artifacts/sentinel/evidence_closure/account_code_identity_migration.json`.

## Verification

- `pytest --cov=uquant --cov-report=json:/tmp/uquant-phase8-coverage-final.json`:
  1,456 passed; exact coverage 85.21829899246619%.
- `ruff check .`: passed.
- `mypy uquant scripts research`: passed for 86 source files.
- authenticated data manifest: 36 files passed; manifest SHA-256
  `343009138d22f8d4a20768f706207fe4d4bcd03581b0c5945c5485ecbd28788d`;
  checksums SHA-256
  `ba460d65f791f238d8a4a16ac62e2225c1832caa6f4da5003166a894edf80e29`.
- `python -m compileall`: passed.
- `uv build`: source distribution and wheel passed.
- Bandit: passed with no findings.
- pip-audit: no known dependency vulnerabilities; the local non-PyPI `uquant`
  package was skipped as expected.

## Production state

Phase 8 ends with Sentinel observational only, `FREEZE_ONLY` as the only
production mode, causal confirmation disabled, and legacy gross-cap mode
rejected explicitly. No new AccountState field, RiskAction, SELL authority,
gross-cap behavior, cooldown, recovery, target, order, fill, or trading path is
introduced.
